{
  "name": "lan-print-relay",
  "version": "1.0.0",
  "description": "Relay server for LAN Print's remote (anywhere) printing — pairs a desktop printer app with phones over the internet using a short ID.",
  "main": "server.js",
  "engines": { "node": ">=18" },
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "ws": "^8.17.0",
    "multer": "^1.4.5-lts.1"
  }
}
